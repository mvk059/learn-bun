{
  "name": "bun-backend-tutorial",
  "version": "1.0.0",
  "description": "A self-paced Bun.sh backend development tutorial building a Task/Project Manager API",
  "scripts": {
    "verify": "bun run verify-solutions.ts",
    "verify:chapter": "bun run verify-solutions.ts --chapter",
    "dev": "bun run --watch chapters/11-deployment/04-configuration-production/solution.ts",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d"
  },
  "dependencies": {
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
